---
# OCIR pull credentials for the agents namespace.
# Identical shape to applications/gemini-sre-agent/ocir-pull-secret.yaml — OCIR pull
# credentials are per-namespace in Kubernetes, so we duplicate the ExternalSecret
# rather than introducing cross-namespace secret sharing.
# Source-of-truth is OCI Vault — the secret `ocir-auth-token` is the single
# place to rotate the token.
#
# Note mcp-github does NOT need this: its image comes from ghcr.io, which is public.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: ocir-pull-secret
  namespace: agents
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: oci-vault
    kind: ClusterSecretStore
  target:
    name: ocir-pull-secret
    creationPolicy: Owner
    template:
      type: kubernetes.io/dockerconfigjson
      data:
        .dockerconfigjson: |
          {
            "auths": {
              "il-jerusalem-1.ocir.io": {
                "username": "axiwhc5fgu9i/shemtov.aaron@gmail.com",
                "password": "{{ .password }}",
                "auth": "{{ printf "%s:%s" "axiwhc5fgu9i/shemtov.aaron@gmail.com" .password | b64enc }}"
              }
            }
          }
  data:
    - secretKey: password
      remoteRef:
        key: ocir-auth-token
